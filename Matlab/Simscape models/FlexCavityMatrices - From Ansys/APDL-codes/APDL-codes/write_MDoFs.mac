! Write Output to txt file
/OUTPUT,out_nodes_3D,txt,c:/ANSYS_temp

! Get maximum node number in model
*GET, max_node, NODE, 0, NUM, MAX,

! Select only master nodes
NSEL, S, M, , 1, max_node

! List node coordinates for output, then reselect original set
NLIST
NSEL, ALL

! List master nodes and corresponding selected DoFs
MLIST
! Re-direct output to terminal
/OUTPUT,TERM